% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc-assume.R
\name{nc_assume}
\alias{nc_assume}
\title{Check assumptions for a negative control analysis}
\usage{
nc_assume(
  ncdata,
  checks = c("exclusion", "u_comparability"),
  model = nc_model(stats::lm),
  ...
)
}
\arguments{
\item{ncdata}{\verb{[nc_data]}\cr
Dataset prepared with \code{\link[=nc_data]{nc_data()}}.}

\item{checks}{\verb{[character()]}\cr
Which assumptions to check. Defaults to all available checks.}

\item{model}{\verb{[nc_model]}\cr
Model specification for regression-based checks. Defaults to OLS.}

\item{...}{\verb{[any]}\cr
Reserved for future use.}
}
\value{
An object of class \code{nc_assume_result}: a named list where each
element corresponds to one check and contains \verb{$passed} (\code{TRUE}/\code{FALSE}/
\code{NA}), \verb{$message}, and optionally \verb{$model_fit}.
}
\description{
\code{nc_assume()} evaluates whether the dataset and specification are consistent
with core assumptions required by negative control methods. Checks are
performed heuristically (e.g. via regression tests) and should be treated
as diagnostic aids, not proofs.
}
\details{
Checks performed:
\itemize{
\item \strong{Exclusion restriction} (\code{"exclusion"}): Tests whether the negative
control exposure (NCE) is associated with the primary outcome after
adjusting for the primary exposure and covariates. A significant
association is evidence against the exclusion restriction.
\item \strong{U-comparability} (\code{"u_comparability"}): Tests whether the NCE is
associated with the primary exposure. A significant association is
consistent with U-comparability: the NCE and the primary exposure share
the same unmeasured confounders.
}
}
\examples{
df <- data.frame(
  A = rbinom(200, 1, 0.5),
  Y = rnorm(200),
  Z = rbinom(200, 1, 0.5),
  W = rnorm(200),
  age = rnorm(200)
)
spec <- nc_spec(
  exposure = "A", outcome = "Y",
  nce = "Z", nco = "W",
  covariates = "age"
)
nd <- nc_data(df, spec)
nc_assume(nd)
}
