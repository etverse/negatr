% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc-correct.R
\name{nc_correct}
\alias{nc_correct}
\title{Correct for bias using negative controls}
\usage{
nc_correct(ncdata, method = "diff_in_diff", model = nc_model(stats::lm), ...)
}
\arguments{
\item{ncdata}{\verb{[nc_data]}\cr
Dataset prepared with \code{\link[=nc_data]{nc_data()}}.}

\item{method}{\verb{[character(1)]}\cr
Correction method to use. See Details for available methods.}

\item{model}{\verb{[nc_model]}\cr
Model specification created with \code{\link[=nc_model]{nc_model()}}. Defaults to OLS via
\code{\link[stats:lm]{stats::lm()}}.}

\item{...}{\verb{[any]}\cr
Additional arguments passed to the specific correction method.}
}
\value{
An \code{\link[=nc_correct_result]{nc_correct_result()}} object.
}
\description{
\code{nc_correct()} applies a bias correction method that uses negative control
variables to produce a bias-adjusted estimate of the primary
exposure-outcome association.
}
\details{
Currently implemented methods:
\itemize{
\item \code{"diff_in_diff"}: Difference-in-differences (simple subtraction). Corrects
the primary estimate by subtracting the negative control association,
under the equi-confounding assumption. Requires a negative control outcome
(\code{nco}).
}
}
\examples{
df <- data.frame(
  A = rbinom(200, 1, 0.5),
  Y = rnorm(200),
  W = rnorm(200),
  age = rnorm(200)
)
spec <- nc_spec(exposure = "A", outcome = "Y", nco = "W", covariates = "age")
nd <- nc_data(df, spec)
nc_correct(nd, method = "diff_in_diff")
}
