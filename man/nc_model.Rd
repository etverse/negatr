% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc-model.R
\name{nc_model}
\alias{nc_model}
\title{Specify a model for a negative control analysis}
\usage{
nc_model(fitter = stats::glm, ...)
}
\arguments{
\item{fitter}{\verb{[function]}\cr
The model fitting function. Defaults to \code{\link[stats:glm]{stats::glm()}}.}

\item{...}{\verb{[any]}\cr
Additional arguments forwarded to \code{fitter} (e.g. \code{family = binomial()}).}
}
\value{
An object of class \code{nc_model}.
}
\description{
\code{nc_model()} describes how to fit regression models within a negative
control analysis. It wraps a fitting function together with any additional
arguments, making the model specification portable and inspectable.
}
\details{
The \code{fitter} must be a function that accepts at minimum a \code{formula} and
\code{data} argument, e.g. \code{\link[stats:glm]{stats::glm()}}, \code{\link[stats:lm]{stats::lm()}}, or a compatible
function from \code{mgcv} or \code{survival}. Custom fitters are also accepted as long
as they follow this convention.
}
\examples{
nc_model()
nc_model(fitter = stats::glm, family = stats::binomial())
nc_model(fitter = stats::lm)
}
