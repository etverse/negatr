% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc-data.R
\name{nc_data}
\alias{nc_data}
\title{Prepare a negative control dataset}
\usage{
nc_data(data, spec)
}
\arguments{
\item{data}{\verb{[data.frame]}\cr
The analysis dataset.}

\item{spec}{\verb{[nc_spec]}\cr
Variable specification created by \code{\link[=nc_spec]{nc_spec()}}.}
}
\value{
An object of class \code{nc_data} (also a \code{data.frame}).
}
\description{
\code{nc_data()} combines a data frame with an \code{\link[=nc_spec]{nc_spec()}} object, validates that
all declared columns are present, and returns an object ready to pass to
\code{\link[=nc_detect]{nc_detect()}} or \code{\link[=nc_correct]{nc_correct()}}.
}
\examples{
df <- data.frame(A = rbinom(100, 1, 0.5), Y = rnorm(100), W = rnorm(100))
spec <- nc_spec(exposure = "A", outcome = "Y", nco = "W")
nc_data(df, spec)
}
